<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ballmazegame.assignment"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_main"
            android:screenOrientation="landscape" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.TiltaballView"
            android:label="@string/title_activity_tiltaball_view" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ballmazegame.assignment.MainActivity" />
        </activity>
        <activity
            android:name=".HowToActivity"
            android:label="@string/title_activity_how_to" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ballmazegame.assignment.MainActivity" />
        </activity>
    </application>

</manifest>